# Agent instructions

Same rules as [CLAUDE.md](CLAUDE.md): understand callers before editing, keep the domain → infrastructure →
application → interfaces layering, one use case behind both MCP and HTTP, build-free frontend, tests per file
before committing, work on a branch with a PR. GitNexus is not used in this fork (license).
